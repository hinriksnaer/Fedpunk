# Fedpunk Mode Detection Data
# This file is generated/updated by the fedpunk installer
# To regenerate: run 'fedpunk-detect-mode' or reinstall

# Mode detection
mode = "desktop"
hostname = "fedora"

# Mode-specific flags (for easy conditionals in templates)
isLaptop = false
isDesktop = true
isContainer = false

# Hardware detection
hasNvidia = true
hasTouchpad = false

# Display configuration (customize per machine)
primaryMonitor = "DP-1"
primaryResolution = "7679x2160@120"
primaryScale = "1.5"
