{{- if and .install.claude (hasKey . "claude") (hasKey .claude "use_vertex") .claude.use_vertex -}}
# Google Vertex AI Configuration for Claude Code
# Enabled via Fedpunk configuration

set -gx CLAUDE_CODE_USE_VERTEX 1
set -gx CLOUD_ML_REGION us-east5
set -gx ANTHROPIC_VERTEX_PROJECT_ID itpc-gcp-ai-eng-claude
{{- end }}
