[module]
name = "dev-tools"
description = "Core development tools: GCC/G++, Make, CMake, Git, and build essentials"
dependencies = ["system-config"]
priority = 12

[lifecycle]

[packages]
dnf = ["git", "fzf", "unzip", "tar", "gcc", "gcc-c++", "make", "cmake", "pkg-config", "openssl-devel"]
cargo = []
npm = []
flatpak = []

[stow]
target = "$HOME"
conflicts = "warn"
