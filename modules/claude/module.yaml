module:
  name: claude
  description: Claude Code with Google Vertex AI authentication
  dependencies: []
  priority: 50

parameters:
  project_id:
    type: string
    description: "Google Cloud project ID for Anthropic Vertex AI"
    default: itpc-gcp-ai-eng-claude

  region:
    type: string
    description: "Google Cloud region for Vertex AI"
    default: us-east5

lifecycle:
  before: []
  after:
    - install

packages:
  dnf:
    - curl
    - libxcrypt-compat
  cargo: []
  npm: []

stow:
  target: $HOME
  conflicts: warn
