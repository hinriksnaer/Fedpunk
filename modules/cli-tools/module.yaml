module:
  name: cli-tools
  description: Essential CLI tools (ripgrep, fd, bat, eza, fzf)

depends:
  - core

hooks:
  before_apply:
    - install.fish

modes:
  container:
    enabled: true
  desktop:
    enabled: true
  laptop:
    enabled: true
