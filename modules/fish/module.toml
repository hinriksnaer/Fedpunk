[module]
name = "fish"
description = "Fish shell setup for Fedpunk - includes Starship, Fisher, and base configuration"
dependencies = ["rust"]
priority = 5

[lifecycle]
install = ["install"]
after = ["setup-fisher"]

[packages]
copr = ["atim/starship"]
dnf = ["fish", "util-linux-user", "starship"]
cargo = []
npm = []
flatpak = []

[stow]
target = "$HOME"
conflicts = "warn"
