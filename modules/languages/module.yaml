module:
  name: languages
  description: Programming language toolchains (Node.js, Python, Go)

depends:
  - core

params:
  install_nodejs: true
  install_python: true
  install_go: true

hooks:
  before_apply:
    - install.fish

modes:
  container:
    enabled: true
  desktop:
    enabled: true
  laptop:
    enabled: true
