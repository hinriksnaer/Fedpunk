# Custom Fish Configuration
# Copy this file to: custom/config.fish (remove .example)
# This file is sourced AFTER the main config, so you can override defaults

# ============================================================================
# Personal Aliases
# ============================================================================

# Git shortcuts
alias gs='git status'
alias ga='git add'
alias gc='git commit'
alias gp='git push'
alias gl='git log --oneline --graph'

# Quick navigation
alias ..='cd ..'
alias ...='cd ../..'
alias dev='cd ~/Development'

# ============================================================================
# Environment Variables
# ============================================================================

# Default applications
set -gx EDITOR nvim
set -gx BROWSER firefox
set -gx TERMINAL kitty

# Custom paths (add your local bin directories)
# set -gx PATH $HOME/my-scripts $PATH

# ============================================================================
# Fedpunk Customization
# ============================================================================

# Override default theme (theme name from themes/ or custom/themes/)
# set -gx FEDPUNK_DEFAULT_THEME "tokyo-night"

# Override default layout (dwindle or master)
# set -gx FEDPUNK_DEFAULT_LAYOUT "master"

# ============================================================================
# Tool Configuration
# ============================================================================

# FZF custom options
# set -gx FZF_DEFAULT_OPTS '--height 40% --border'

# LS colors customization
# set -gx LS_COLORS 'di=34:ln=35:so=32:pi=33:ex=31'

# ============================================================================
# Custom Functions
# ============================================================================

# Example: Quick project opener
# function proj
#     cd ~/Development/$argv[1]
# end

# Example: Git commit with conventional commits
# function gcommit
#     git commit -m "$argv"
# end

# ============================================================================
# Conditional Setup
# ============================================================================

# Example: Different config for work machine
# if test (hostname) = "work-laptop"
#     set -gx WORK_MODE true
#     # Work-specific aliases and config
# end

# Example: Load work-specific secrets
# if test -f ~/.work-secrets.fish
#     source ~/.work-secrets.fish
# end
