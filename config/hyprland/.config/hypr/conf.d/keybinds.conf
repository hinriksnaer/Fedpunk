# Key Bindings
# All keyboard shortcuts and mouse bindings
# Organized by category for easy navigation

$mainMod = SUPER

# ================================
# Applications
# ================================
bind = $mainMod, Return, exec, kitty                             # Terminal
bind = $mainMod SHIFT, Return, togglespecialworkspace, emergency # Emergency terminal
bind = $mainMod, B, exec, firefox                                # Browser
bind = $mainMod, E, exec, thunar                                 # File manager
bind = $mainMod, Space, exec, $launcher                          # App launcher (see variables.conf)
bind = $mainMod SHIFT, B, exec, $HOME/.local/share/fedpunk/bin/fedpunk-bluetooth  # Bluetooth TUI

# ================================
# Window Management
# ================================
bind = $mainMod, Q, killactive,                   # Close window
bind = $mainMod SHIFT, E, exit,                   # Exit Hyprland
bind = $mainMod, V, togglefloating,               # Toggle floating
bind = $mainMod, F, fullscreen,                   # Toggle fullscreen
bind = $mainMod, P, pseudo,                       # Pseudo tile
bind = $mainMod, J, togglesplit,                  # Toggle split

# Layout toggle (dwindle <-> master for ultrawide)
bind = SUPER_ALT, L, exec, fish $HOME/.config/hypr/scripts/toggle-layout.fish

# Theme switcher (cycle through available themes)
bind = $mainMod CTRL, T, exec, $HOME/.local/share/fedpunk/bin/rofi-theme-select        # Theme selection menu (rofi)
bind = $mainMod SHIFT, T, exec, $HOME/.local/share/fedpunk/bin/fedpunk-theme-next       # Next theme
bind = $mainMod SHIFT, Y, exec, $HOME/.local/share/fedpunk/bin/fedpunk-theme-prev       # Previous theme
bind = $mainMod SHIFT, R, exec, $HOME/.local/share/fedpunk/bin/fedpunk-theme-refresh    # Refresh current theme

# Debug tools
bind = $mainMod SHIFT, D, exec, kitty -e fish ~/.config/hypr/scripts/debug-startup.fish   # Startup diagnostics

# Wallpaper cycling
bind = $mainMod SHIFT, W, exec, $HOME/.local/share/fedpunk/bin/fedpunk-wallpaper-next  # Next wallpaper

# ================================
# Window Focus (Arrow Keys)
# ================================
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# ================================
# Window Focus (Vim Keys)
# ================================
bind = $mainMod, h, movefocus, l
bind = $mainMod, l, movefocus, r
bind = $mainMod, k, movefocus, u
bind = $mainMod, j, movefocus, d

# ================================
# Window Movement (Shift + Vim Keys)
# ================================
bind = $mainMod SHIFT, h, movewindow, l
bind = $mainMod SHIFT, l, movewindow, r
bind = $mainMod SHIFT, k, movewindow, u
bind = $mainMod SHIFT, j, movewindow, d

# ================================
# Window Resize (Ctrl + Vim Keys)
# ================================
bind = $mainMod CTRL, h, resizeactive, -50 0
bind = $mainMod CTRL, l, resizeactive, 50 0
bind = $mainMod CTRL, k, resizeactive, 0 -50
bind = $mainMod CTRL, j, resizeactive, 0 50

# ================================
# Workspace Switching
# ================================
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Scroll through workspaces
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# ================================
# Move Window to Workspace
# ================================
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Move window to workspace silently (doesn't switch focus)
bind = $mainMod CTRL, 1, movetoworkspacesilent, 1
bind = $mainMod CTRL, 2, movetoworkspacesilent, 2
bind = $mainMod CTRL, 3, movetoworkspacesilent, 3
bind = $mainMod CTRL, 4, movetoworkspacesilent, 4
bind = $mainMod CTRL, 5, movetoworkspacesilent, 5

# ================================
# Mouse Bindings
# ================================
bindm = $mainMod, mouse:272, movewindow           # LMB - Move window
bindm = $mainMod, mouse:273, resizewindow         # RMB - Resize window

# ================================
# Media Keys
# ================================
bind = , XF86AudioRaiseVolume, exec, $HOME/.local/share/fedpunk/bin/volume-control up
bind = , XF86AudioLowerVolume, exec, $HOME/.local/share/fedpunk/bin/volume-control down
bind = , XF86AudioMute, exec, $HOME/.local/share/fedpunk/bin/volume-control mute
bind = , XF86AudioPlay, exec, playerctl play-pause
bind = , XF86AudioPause, exec, playerctl play-pause
bind = , XF86AudioNext, exec, playerctl next
bind = , XF86AudioPrev, exec, playerctl previous

# ================================
# Screenshot Bindings
# ================================
bind = , Print, exec, grim -g "$(slurp)" - | wl-copy          # Selection
bind = $mainMod, Print, exec, grim - | wl-copy                # Full screen
bind = $mainMod SHIFT, S, exec, grim -g "$(slurp)" - | wl-copy # Alt selection
