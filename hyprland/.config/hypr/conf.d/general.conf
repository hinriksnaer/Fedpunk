# General Settings
# Gaps, borders, and layout selection

general {
    gaps_in = $gaps_in
    gaps_out = $gaps_out
    border_size = $border_size

    layout = dwindle

    # Allow tearing for reduced latency (gaming)
    allow_tearing = false
}
